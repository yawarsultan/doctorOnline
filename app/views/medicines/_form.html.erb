<%= form_for(medicine) do |f| %>
  <% if medicine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(medicine.errors.count, "error") %> prohibited this medicine from being saved:</h2>

      <ul>
      <% medicine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    

    <%= f.text_field :name, class: "txt", placeholder: "Medicine Name" %>

    <%= f.text_area :weight, class: "txtarea", placeholder: "Weight of Medicine" %>
    
    <%= f.text_area :description, class: "txtarea", placeholder: "Information about medicine" %>

    <%= f.submit "Add Medicine", class: "btn btn-primary" %>
<% end %>
